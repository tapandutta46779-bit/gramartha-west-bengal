# Price Model

The interface expects a local, source-linked `milk_price_inr_per_litre` interval. No current local
series is loaded, so `NO_PRICE_EVIDENCE` blocks a recommendation. CPI/WPI is not treated as demand
or a direct local milk quotation. AGMARKNET/e-NAM and fuel-series ingestion remains pending.

