# Robust Selection

Finite-scenario minimax regret is exact over the explicitly supplied value table and feasibility
mask. It reports every candidate's maximum regret. Base-case, minimum-capital and survival-first
views are not yet integrated into ordinary real-data analysis.

